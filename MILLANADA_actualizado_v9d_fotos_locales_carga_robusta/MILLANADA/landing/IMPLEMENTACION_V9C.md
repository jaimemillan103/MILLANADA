# MILLANADA v9c

Cambios respecto a v9b:

- Las 15 fotos del Recap ya no se cargan desde Google Drive en el navegador.
- Se alojan localmente en `public/recap/` como WebP optimizadas, evitando bloqueos de Drive y mejorando rendimiento.
- Se elimina la frase bajo el carrusel que explicaba que se mueve solo.
- Se mantiene el nuevo `/exec` de Apps Script en `wrangler.jsonc`.

Para publicar: sustituir el contenido del repo por esta versión o copiar los archivos modificados, commit a `main` y esperar el deploy de Cloudflare.
